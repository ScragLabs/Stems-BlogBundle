<div class="widget widget-latest-post">
	<a href="/magazine/{{ post.slug }}" style="background:url('{{ getMediaImage(post.image).src }}') center center; background-size:cover;">
		<p>Magazine:</p>
		<h2>{{ post.title }}<span>{{ post.excerpt }}</span></h2>
	</a>
</div>