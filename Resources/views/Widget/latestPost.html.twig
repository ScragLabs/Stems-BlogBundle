<div class="widget widget-latest-post">
	<a href="/magazine/{{ post.slug }}" style="background:url('{{ post.image }}') center center; background-size:cover;">
		<p>Magazine:</p>
		<h2>{{ post.title }}<span>{{ post.excerpt }}</span></h2>
	</a>
</div>